# 技术架构概述

## 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                        前端 (Frontend)                       │
│  React 19 + TypeScript + Vite + Ant Design + React Query    │
└─────────────────────────────┬───────────────────────────────┘
                              │ HTTP/REST
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                        后端 (Backend)                        │
│              FastAPI + SQLAlchemy + JWT Auth                │
└─────────────────────────────┬───────────────────────────────┘
                              │ SQL
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                       数据库 (Database)                      │
│                      MySQL 8.0 (InnoDB)                     │
└─────────────────────────────────────────────────────────────┘
```

## 技术选型

### 后端

| 组件 | 技术 | 版本 | 说明 |
|------|------|------|------|
| Web 框架 | FastAPI | 0.109 | 高性能异步框架 |
| ORM | SQLAlchemy | 2.0 | 数据库映射 |
| 数据库 | MySQL | 8.0 | 关系型数据库 |
| 数据库驱动 | PyMySQL | 1.1 | MySQL 连接器 |
| 数据验证 | Pydantic | 2.5 | 数据模式验证 |
| 认证 | python-jose | 3.3 | JWT 令牌 |
| 密码 | passlib | 1.7 | bcrypt 加密 |
| 测试 | pytest | 7.4 | 单元测试框架 |
| 导出 | openpyxl | 3.1 | Excel 生成 |

### 前端

| 组件 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 框架 | React | 19 | UI 框架 |
| 语言 | TypeScript | 5.9 | 类型安全 |
| 构建 | Vite | 7 | 快速构建工具 |
| UI 库 | Ant Design | 6 | 企业级组件库 |
| 状态管理 | React Query | 5 | 服务端状态 |
| HTTP | Axios | 1.13 | HTTP 客户端 |
| 地图 | Leaflet | 1.9 | 开源地图库 |
| 日期 | Day.js | 1.11 | 日期处理 |

## 多租户架构

### 数据隔离

采用**共享数据库 + 租户 ID 隔离**模式：

```
┌─────────────────────────────────────────┐
│              共享数据库                  │
├─────────────────────────────────────────┤
│  tenants     │ 租户表                   │
│  users       │ 用户表 (tenant_id)       │
│  customers   │ 客户表 (tenant_id)       │
│  templates   │ 模板表 (tenant_id)       │
│  fields      │ 字段表 (tenant_id)       │
│  roles       │ 角色表 (tenant_id)       │
└─────────────────────────────────────────┘
```

### 隔离实现

1. **中间件层**: 从 JWT 解析租户 ID
2. **查询层**: 所有查询自动添加 `tenant_id` 过滤
3. **权限层**: 校验用户是否属于当前租户

## 权限系统 (RBAC)

### 角色层级

```
超级管理员 (is_superadmin=True)
    │
    └── 租户管理员 (is_admin=True)
            │
            └── 普通用户 (角色权限)
```

### 数据权限 (data_scope)

| 范围 | 说明 |
|------|------|
| `self` | 只能看自己的数据 |
| `team` | 能看自己和下属的数据 |
| `all` | 能看租户内所有数据 |

### 权限码

```python
permissions = [
    "customer:create",
    "customer:read",
    "customer:update",
    "customer:delete",
    "customer:export",
    "template:manage",
    "field:manage",
    "user:manage",
    "role:manage",
]
```

## 核心数据模型

### 实体关系

```
Tenant (租户)
  │
  ├── User (用户) ─── Role (角色)
  │
  ├── FieldDefinition (字段定义)
  │
  ├── CustomerTemplate (客户模板)
  │     │
  │     └── TemplateField (模板字段关联)
  │
  └── Customer (客户)
        │
        └── CustomerFieldValue (字段值)
```

### 动态表单设计

```
FieldDefinition (字段定义)
├── field_key: 字段键名
├── field_name: 显示名称
├── field_type: 类型 (text/number/select/date/...)
└── options: 选项配置 (JSON)

CustomerTemplate (客户模板)
├── template_fields: 关联的字段列表
└── 支持配置必填、排序

Customer (客户)
├── template_id: 使用的模板
└── field_values: 动态字段值 (JSON)
```

## API 设计

### RESTful 规范

```
GET    /api/customers          # 列表
POST   /api/customers          # 创建
GET    /api/customers/{id}     # 详情
PUT    /api/customers/{id}     # 更新
DELETE /api/customers/{id}     # 删除
```

### 认证流程

```
1. POST /api/auth/login → 获取 JWT Token
2. 请求头携带: Authorization: Bearer {token}
3. 服务端验证 token，解析 user_id 和 tenant_id
```

## 部署架构

### 开发环境

```
前端: npm run dev (Vite, port 5173)
后端: uvicorn --reload (port 8000)
数据库: MySQL 本地实例
```

### 生产环境（建议）

```
前端: Nginx 静态托管
后端: Gunicorn + Uvicorn workers
数据库: MySQL 主从 / 云数据库
```
